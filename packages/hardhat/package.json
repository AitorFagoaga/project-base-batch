{
  "name": "@meritocratic-launchpad/hardhat",
  "version": "1.0.0",
  "private": true,
  "description": "Smart contracts for Meritocratic Launchpad",
  "scripts": {
    "compile": "hardhat compile",
    "build": "npm run compile && npm run export-abis",
    "export-abis": "hardhat run scripts/export-abis.ts",
    "test": "hardhat test",
    "deploy": "hardhat ignition deploy ignition/modules/Deploy.ts --network baseSepolia",
    "deploy:local": "hardhat ignition deploy ignition/modules/Deploy.ts --network localhost",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-ignition": "^0.15.0",
    "@nomicfoundation/hardhat-ignition-ethers": "^0.15.0",
    "@types/node": "^22.0.0",
    "hardhat": "^2.22.16",
    "typescript": "^5.6.0"
  }
}
